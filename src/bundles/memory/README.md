# lfx-memory

Memory components as a standalone Langflow Extension Bundle.

## Install

```bash
pip install lfx-memory
```

The bundle is registered automatically through the `langflow.extensions`
entry point. Restart Langflow after installation to load its components.
